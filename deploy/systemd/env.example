# Fox Control environment — installed to /etc/fox-control/env by install.sh.
# Set real values before starting the service. Empty values cause startup to fail.

FOX_ADMIN_SECRET=
FOX_INSTANCE_PASSWORD=
